<?php

return [

    'cheatsheet' => '
<p>Markdown هو بناء جملة ترميز النص إلى HTML لكتاب الويب. يتيح لك Markdown إمكانية الكتابة باستخدام تنسيق نص عادي سهل القراءة وسهل القراءة يتم تحويله إلى HTML صالح بنيوياً.</p>

<h3>ترويسات</h3>
<pre class="language-markdown"><code># هذا h1
## هذا h2
### هذا h3, إلخ...
</code></pre>

<h3>غامق &amp; مائل</h3>
<pre class="language-markdown"><code>يمكنك جعل الأشياء * مؤكدة * ، ** جريئة ** ، أو _ ** كلاهما ** _.</code></pre>

<h3>الروابط</h3>
<pre class="language-markdown"><code>هذا [رابط مثال](http://example.com).</code></pre>

<h3>الشفرات</h3>
<p>
لف الكود الخاص بك مع 3 باكتكس (<code>```</code>) على الخط قبل وبعد.
</p>

<pre class="language-markdown"><code>```
this: is some yaml
```</code></pre>

<p>يمكنك أيضًا إضافة رمز مضمن من خلال التفاف المحتوى باستخدام <code>`</code> backtick واحد.

<h3>الإقتباس</h3>

<p>قم بإنشاء فقرة منقوطة من خلال بدء النص الخاص بك باستخدام <code>> </code>.</p>

<pre class="language-markdown"><code>> هذا سيكون مقدما.</code></pre>

<h3>الصور</h3>
<pre class="language-markdown"><code>![نص بديل](http://example.com/image.jpg)</code></pre>

<h3>قائمة غير مرتبة</h3>
<pre class="language-markdown"><code>- لحم مقدد
- شريحة لحم
- جعة</code></pre>

<h3>قوائم مرتبة</h3>
<pre class="language-markdown"><code>1. الطعام
2. الشراب
3. الزواج</code></pre>

<h3>الجداول</h3>

<pre class="language-markdown"><code>الترويسة الأولى  | الترويسة الثانية
------------- | -------------
محتوى خلية  | محتوى خلية
محتوى خلية  | محتوى خلية</code></pre>'

];
